# Environment example for sw2-ml microservice
# ===============================================
# Copiar a .env y ajustar valores según tu entorno

# --- LOCAL TESTING (Docker Compose) ---
# Usa host.docker.internal para alcanzar servicios en el host
# PLAZOS_ENDPOINT=http://host.docker.internal:3000/plazos
# DOCS_ENDPOINT=http://host.docker.internal:8081/admin/documentos

# --- PRODUCTION (Cloud deployment) ---
# Usa IPs públicas o URLs de tus microservicios
# Expedientes en DigitalOcean:
PLAZOS_ENDPOINT=http://129.212.136.101/plazos
# Documentos en Azure:
DOCS_ENDPOINT=http://128.203.103.88/admin/documentos

# Nombres alternativos (compatibilidad con otras configs)
EXPEDIENTES_URL=http://129.212.136.101
DOCUMENTOS_URL=http://128.203.103.88

# --- Auth (si gateway/backends requieren token) ---
SERVICE_AUTH_TOKEN=
JWT_SECRET=
JWT_ALGORITHM=HS256

# --- Timeouts y retries ---
TIMEOUT_MS=30000
RETRIES=3

# --- Networking / server ---
HOST=0.0.0.0
PORT=8010

# --- Tuning (limita threads de NumPy/scikit-learn) ---
OMP_NUM_THREADS=1

# --- Google Cloud Run (opcional) ---
# Estas variables las configuras en el deployment con --set-env-vars
# GCP_PROJECT_ID=sw2-ml-service-123456
# GCP_REGION=us-central1
# GCP_SERVICE_NAME=sw2-ml-service
