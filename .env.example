# Environment example for sw2-ml microservice (local testing)
# Upstream endpoints (the API Gateway guide uses EXPEDIENTES_URL and DOCUMENTOS_URL)
EXPEDIENTES_URL=http://host.docker.internal:3000
DOCUMENTOS_URL=http://host.docker.internal:8081
# Alternative names supported by this service
PLAZOS_ENDPOINT=http://host.docker.internal:3000/plazos
DOCS_ENDPOINT=http://host.docker.internal:8081/admin/documentos

# Auth (if your gateway/backends require a token)
SERVICE_AUTH_TOKEN=
JWT_SECRET=
JWT_ALGORITHM=HS256

# Requests / retries
TIMEOUT_MS=30000
RETRIES=3

# Networking / server
HOST=0.0.0.0
PORT=8010

# Tuning
OMP_NUM_THREADS=1
